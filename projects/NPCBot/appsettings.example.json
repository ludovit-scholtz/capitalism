{
  "_comment": "Copy to appsettings.Local.json (git-ignored) and fill in a real BotPassword OR set ApiKey.",
  "NpcBot": {
    "GraphqlUrl": "https://capitalism.de-4.biatec.io/graphql",
    "BotCount": 3,
    "Enabled": true,
    "BotNamePrefix": "NPC",
    "BotEmailDomain": "npcbot.capitalism.local",
    "PollIntervalSeconds": 60,
    "MaxConsecutiveErrors": 5,
    "TokenRefreshBufferMinutes": 5,
    "AllowedIndustries": [
      "FURNITURE",
      "FOOD_PROCESSING",
      "HEALTHCARE"
    ],

    "_credential_comment": "Choose ONE of the two options below:",

    "_option_1_password": "Set a strong, unique password (min 8 chars) for password-based bot auth.",
    "_option_1_generate": "Generate with: openssl rand -hex 32",
    "BotPassword": "<replace-with-strong-secret>",

    "_option_2_apikey": "OR set an API key (from the admin dashboard) and leave BotPassword empty.",
    "ApiKey": ""
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft": "Warning",
      "System": "Warning"
    }
  }
}
