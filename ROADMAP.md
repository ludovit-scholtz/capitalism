# Capitalism Roadmap

Create a fun game in the style of Capitalism II, where players experience realistic market dynamics, strategy trade-offs, and fair competitive mechanics without exploit paths.

## Active issues to work on

### Onboarding

- [x] Remove the company name selection and person name selection from the onboarding. Keep auto generated name, but hide the name selection. Users can change the name later in the game.
- [x] On mobile, when user clicks on any button in the onboarding after he scrolled little down, the screen goes to top which creates undesired confusion.
- [x] In the purchase factory step and purchase sales shop, make one of the recommended choices selected one, so that user can just click continue.
- [x] In the sales shop purchase onboarding step show on map the distance from the factory. The main point is to optimize the distance between the factory while tuning up the retail index

### Copy-Paste units

- [x] Copy-paste of the units works fine on desktop with keyboard, however this feature is not available for small devices at the moment. Add on the grid page also copy and paste buttons when building is in editation mode.

### News

- [x] Add pagination to each category of news. At each category make sure to show top 10 recent news. At the moment if there if there is more then 10 news from reporting category and i select the changelog category, it does not show any items.

### Emails

- [x] Setup email using Email Communication Services in azure
- [x] Create templates for emails using handlebars rendered from the html template file. All emails must have same design. Create professional looking email template. Make sure each localization works for every supported language.
- [x] When user never received email (create flag in the master database), send him the registration email. In the email also write his current url address which he accessed.
- [x] Send users email on weekly basis in friday noon with the report where will be listed all their active game servers, their profit and ranking in the game server, then the master server bounties points they collected in past week, and if there are any news from the changelog within a week add it there.
- [x] Store the language preference after the user logs in to the system to the master server database, and use that localization for the emails to be sent. If no language is set in the database use English.

### Account deletion

- [x] Add a Danger Zone in the master frontend user settings with a delete account section that requires the user to confirm by entering their email address, lists what they lose (game progress, tokenized gold deposits, future tokenized gold rewards) and warns that all game-server data will be removed.
- [x] Do not delete immediately: mark the account for deletion with a 24-hour cooldown that the user can cancel, and send a request email and a final confirmation email (both polite, localized, with the master portal link).
- [x] When purging game-server data destroy all of the user's buildings except banks, transfer banks to the government entity, and set deposit interest to 0% and lending interest to 20%.
- [x] Keep it secure (only the user can delete their own account) with backend and e2e test coverage, and update the in-game user documentation.

### Security improvements

- [x] Fix company-merge tax evasion in `projects/Api/Types/Mutation.CompanyMerge.cs`: clamp the merge-time tax to the target's available balance (`Math.Min(balance, taxAmount)`), assert the `TryDebit` result, and record only the amount actually paid so cash-poor companies cannot merge to escape tax. Regression test for the cash-poor merge scenario added.
- [x] Harden MasterApi auth rate limiting in `projects/MasterApi/Security/AuthRateLimitMiddleware.cs` to parse selected GraphQL root fields across named operations and JSON-array batched bodies, matching the game API, so batched/named login and register requests cannot bypass the per-IP limiter. Regression tests cover the bypass shapes.
- [x] Make GraphQL batching explicit on MasterApi in `projects/MasterApi/Security/GraphQlRequestSecurityMiddleware.cs`: every batch item is iterated and the same introspection, depth, and complexity checks the game API performs are applied before execution. Regression tests added.
- [x] Remove raw JWT persistence from the game frontend `projects/frontend/src/stores/auth.ts` (`auth_token`/`auth_expires` in `localStorage`) and rehydrate gameplay sessions from the cookie session instead. Tests for login, OIDC callback, reload, and logout added.
- [x] Audit and standardize the remaining unchecked `CompanyBankingService.TryDebit`/`TryCredit` call sites in `projects/Api/Engine/Phases` for the same ignored-failure-plus-phantom-ledger pattern, adopting the `Math.Min(balance, due)` clamp-and-record convention used by `TaxPhase` (applied to `SupplyContractFulfillmentPhase` under-delivery penalties and `TradeRoutePhase` shipping costs).
- [x] Upgrade DOMPurify to ≥3.4.11 in both frontends: run `npm audit fix` in `projects/frontend` and `projects/master-frontend`, update the semver lower bound to `"^3.4.11"` in both `package.json` files, and confirm zero production vulnerabilities with `npm audit --omit=dev`. Resolves 8 CVEs (GHSA-x4vx-rjvf-j5p4 and related) in the library used to sanitize news and support-ticket HTML before `v-html` rendering. Current app usage (standard `sanitize()` mode) is not directly exploitable, but the dependency must be current before any future use of IN_PLACE or setConfig. Verified fixed (3.4.11 installed) in the 2026-07-07 audit.
- [x] Upgrade Vite to ≥7.3.4 in both frontends (`npm audit fix` resolves this alongside DOMPurify): fixes GHSA-v6wh-96g9-6wx3 (NTLMv2 hash disclosure via UNC path in launch-editor on Windows) and GHSA-fx2h-pf6j-xcff (server.fs.deny bypass on Windows alternate paths). Dev-only risk but affects developer workstations running `npm run dev` on Windows. Verified fixed (7.3.5 installed) in the 2026-07-07 audit.
- [x] Enforce OIDC HTTPS metadata outside Development: `"RequireHttpsMetadata": true` set in the base `appsettings.json` of both `projects/Api` and `projects/MasterApi`, `MasterApi/Security/BiatecOidcOptions.RequireHttpsMetadata` now defaults to `true`, and a fail-fast non-Development/Testing startup guard (`RequiredSecretsStartupGuard.TryGetUnsafeOidcHttpsMetadataReason`) blocks startup when `BiatecOidc:Enabled=true` and either `RequireHttpsMetadata=false` or the Authority is not HTTPS. Regression tests added in `Api.Tests/OidcHttpsMetadataStartupGuardHostTests.cs` and `MasterApi.Tests/OidcHttpsMetadataStartupGuardHostTests.cs`.
- [x] Added a weekly scheduled (cron) trigger to `api-ci-cd.yml`, `frontend-ci-cd.yml`, and `deploy-stage-k8s.yml` (which builds and deploys MasterApi/master-frontend) so Docker images are rebuilt and redeployed even when `main` has no code pushes, ensuring runtime security patches (e.g. CVE-2026-45591, fixed in .NET 10.0.9) ship on a regular cadence. Production master deploy remains manual (`workflow_dispatch`) by existing design; not changed here.
- [x] Fixed nginx `add_header` inheritance in `projects/frontend/nginx.conf` and `projects/master-frontend/nginx.conf`: every location that declares its own `add_header` (static assets, `/sw.js`, `/health`) now repeats the full security-header set so asset responses keep `nosniff` and HSTS; added the missing `Permissions-Policy` header to the game frontend. Verified with `npm run test:security-headers` in both frontends.
- [ ] Clear esbuild advisory GHSA-g7r4-m6w7-qqqr (dev-server arbitrary file read on Windows, low, dev-tooling only) in both frontends at the next dependency cycle: run `npm audit fix` or bump Vite to a release that lifts esbuild past 0.28.0, then re-run `npm audit` to confirm zero advisories. Attempted 2026-07-07 via an `overrides` pin to `esbuild@^0.28.1`, but `npm install` could not complete in the sandboxed audit environment (registry fetches for the platform-specific `@esbuild/*` optional packages were silently dropped mid-run); needs a normal dev machine or CI runner with full network access.

### Flutter mobile app

The scaffold in `projects/flutter_app` (routing, nav drawer, theming, i18n plumbing, GraphQL client, Bearer-token auth state) was generated without the Flutter SDK available and ships every screen as an empty placeholder. Native platform runners still need generating, and each screen below needs its real implementation, ported from the matching Vue view in `projects/frontend/src/views/`.

- [x] Generate native platform runners for `projects/flutter_app` by running `flutter create .` (Android/iOS/Web/Windows) now that the Dart source, pubspec, and l10n config exist; verified the committed `lib/`, `pubspec.yaml`, `README.md`, `analysis_options.yaml`, and `.gitignore` were untouched (checksum diff before/after). `flutter analyze` is clean, `flutter build web` succeeds, and `flutter test` (7 tests, incl. drawer/bottom-nav navigation coverage) passes. Building for Android/iOS/Windows still needs their native toolchains (Android SDK; Visual Studio "Desktop development with C++" workload) installed on the dev machine — not available in the environment this was verified in.
- [x] Wire `url_launcher` for the Discord nav link and build the Chat side-panel/screen in `lib/core/widgets/app_shell.dart` (`_handleTap`): Discord now opens via an injectable `UrlOpener` (`lib/core/services/url_opener.dart`), and Chat opens `lib/features/chat/chat_panel.dart` as a modal bottom sheet, matching the web's non-routed side panel. Covered by `test/feature_actions_test.dart`.
- [x] Build a native Biatec OIDC sign-in flow (`lib/core/auth/biatec_oidc_service.dart`, using `flutter_web_auth_2` — Custom Tabs/`ASWebAuthenticationSession` on Android/iOS, system browser + loopback listener on Windows/Linux) to complement the Bearer-JWT auth already wired in `lib/core/auth/auth_state.dart`. Replicates the web's implicit-flow protocol (`response_type=id_token`, `response_mode=query`) and the same client-side state/nonce/issuer/audience checks from `projects/frontend/src/stores/auth.ts`; the id_token becomes the Bearer token directly, no backend changes needed. Wired into a "Sign in with Biatec" button on the Sign In screen. Android needs the `com.linusu.flutter_web_auth_2.CallbackActivity` intent filter added to `AndroidManifest.xml` (done) for the `io.biatec.capitalism` callback scheme; iOS needs no manifest entry. Covered by `test/biatec_oidc_service_test.dart` (state/nonce/issuer/audience validation, plus a test that forces `debugDefaultTargetPlatformOverride` through Android/iOS/Windows to assert the redirect URI shape differs correctly per platform) and `test/feature_actions_test.dart` (button wiring). Still open: a real Biatec IdP to test the flow against end-to-end (validated via fakes only), and macOS/Linux weren't exercised.
- [x] Implement the Home screen (`lib/features/home/home_screen.dart`, mirrors `HomeView.vue`): hero + auth-dependent CTA ("Get Started" / "Go to Dashboard" — the web's third CTA state additionally needs `player.onboardingCompletedAtUtc`, not available until the `me` query is wired up), tick/tax-rate status cards, and a top-5 leaderboard preview, all fetched via one `HomeStatus` GraphQL query (`gameState { currentTick taxRate }`, `rankings { displayName totalWealthUsd }` — field names verified against `Api/Data/Entities/GameState.cs` and `Api/Types/Query.Types.Rankings.cs`) with loading/error/retry states. Covered by `test/navigation_test.dart`.
- [x] Implement the Sign In screen (`lib/features/auth/auth_screens.dart` `LoginScreen`, mirrors `LoginView.vue`): email/password Login↔Register toggle, client-side validation, real `Login`/`Register` GraphQL mutations against the **Master API** (`AppConfig.masterGraphqlUrl`, not the game API), and the same server error-code → message mapping as the web (`LOGIN_THROTTLED`, `INVALID_CREDENTIALS`, `REGISTRATION_FAILED`, `AUTH_PASSWORD_DISABLED`, `INVALID_EMAIL`, `DISPLAY_NAME_REQUIRED`, `PASSWORD_TOO_SHORT` — verified against `MasterApi/Types/Mutation.Auth.cs`). Also matches a genuinely surprising web default: `passwordAuthEnabled` mirrors `VITE_AUTH_PASSWORD_ENABLED`, which defaults to **false** — production shows a Biatec-OIDC-only screen with an auto-redirect unless password auth is explicitly enabled (`--dart-define=AUTH_PASSWORD_ENABLED=true`). Trimmed from the web version: no referral-code banner (the web store sends a `referralCode` field the server's `RegisterInput` doesn't actually have — dead code there, not ported), no auto-generated display-name suggestion, no `oidc_retry=consent` drive-access-hint flow.
- [x] Implement the Forgot Password screen (`lib/features/auth/auth_screens.dart` `ForgotPasswordScreen`, mirrors `ForgotPasswordView.vue`): single email field, **REST** (not GraphQL) `POST {masterApiOrigin}/auth/forgot-password`, generic anti-enumeration success message shown in place (no redirect), `METHOD_NOT_ALLOWED` mapped to an OIDC-only message — verified against `MasterApi/Program.cs`.
- [x] Implement the Reset Password screen (`lib/features/auth/auth_screens.dart` `ResetPasswordScreen`, mirrors `ResetPasswordView.vue`): token read from the `?token=` query param (not a path segment) via `app_router.dart`, new/confirm password fields with a live weak/medium/strong strength label, client-side missing-token and password-mismatch checks that skip the network call entirely, **REST** `POST {masterApiOrigin}/auth/reset-password`, and a 2-second auto-redirect to `/login` on success (no auto-login) — verified against `MasterApi/Program.cs`, including that the server deliberately returns one combined `RESET_TOKEN_INVALID_OR_EXPIRED` code rather than distinguishing invalid vs. expired vs. already-used.
- [x] Implement the Auth Callback screen (`lib/features/auth/auth_screens.dart` `AuthCallbackScreen`, mirrors `AuthCallbackView.vue`) — architecturally adapted rather than byte-for-byte ported: on the web this screen only *parses* an already-completed browser redirect back from the IdP, because `startBiatecOidcSignIn` does a full-page `window.location.assign` away from `LoginView`. Native has no such round trip — `flutter_web_auth_2` returns the callback URL directly to the caller — so this screen now *owns* the `BiatecOidcService.signIn()` call itself, and `LoginScreen`'s Biatec button (and its OIDC-only auto-redirect) navigate here rather than signing in directly. This makes the split of responsibility match the web (Login only starts the flow; Callback shows loading/success/error) even though the trigger mechanism differs. Still handles a provider error arriving via `?error=&error_description=` query params without calling the authenticator, matching `detectOidcProviderError()`.
- [x] Implement the Onboarding screen (`lib/features/onboarding/onboarding_screen.dart` + `onboarding_models.dart`/`onboarding_service.dart`/`onboarding_steps.dart`/`onboarding_complete_step.dart`, mirrors `OnboardingView.vue`): all 7 steps (city → industry → product → IPO → factory-lot purchase → shop-lot purchase → completion), the real two-mutation contract (`startOnboardingCompany` then `finishOnboarding` — verified against `Api/Types/Mutation.Onboarding*.cs`, not the legacy one-shot `completeOnboarding` mutation the current web wizard no longer calls), guest mode (steps 1–6 fully completable with zero backend calls, local cash/lot simulation, then a "Save Progress" button that signs in via the existing Biatec flow and migrates by calling both mutations with the guest's saved selections, all within the same widget instance so the selections survive the sign-in round trip), and backend-driven resume (`me { onboardingCurrentStep, onboardingIndustry, onboardingCityId, onboardingFactoryLotId, ... }` — landing directly on step 6 when `onboardingCurrentStep == 'SHOP_SELECTION'`, on step 7 when completed but no first sale yet, or redirecting to `/dashboard` when fully done). Fixed IPO plans and the starter-industry/product Pro-gating map are hardcoded to match the web and the backend's `StarterOnboardingProductsByIndustry`. `LOT_ALREADY_OWNED` recovery (clear selection, reload lots) is implemented for both lot-purchase steps. Covered by `test/onboarding_screen_test.dart` (7 tests: full authenticated happy path with exact mutation-arg assertions, Pro-industry gating, lot-conflict recovery, backend resume into step 6, completed-onboarding redirect to dashboard, milestone completion, and the guest no-mutations-until-save-then-migrates flow).
  - Deliberately trimmed from the web version (a large wizard — see `ROADMAP.md` history/PR description for the full research this was scoped against): a list-based lot picker instead of the interactive map, a simplified two-cheapest-unowned-lots "recommended" heuristic instead of the web's district-name-regex + population-index rules, no FX-precise currency conversion (figures shown as raw USD-equivalent numbers, not converted to city local currency), simple local company/person name generation instead of the web's exact wordlists (and no regeneration when city/industry changes — generated once per session), and no auto-polled first-sale-mission celebration panel or per-industry "configure guide" price-benchmark cards (the "mark milestone complete" button still calls the real `completeFirstSaleMilestone` mutation, just without the live tick-polling auto-detection). Real Pro-subscription gating isn't wired up (no subscription data available yet) — Pro-only industries are simply always blocked with a message, for both guests and authenticated players.
- [x] Implement the Dashboard screen (`lib/features/dashboard/dashboard_screen.dart` + `dashboard_models.dart`/`dashboard_service.dart`/`dashboard_widgets.dart`, mirrors `DashboardView.vue`): the same guard sequence as the web (unauthenticated → `/login`; `onboardingCompletedAtUtc` unset → `/onboarding`; deferred to a post-frame callback since calling `context.go()` synchronously from `initState()` before any `await` fights with the Router mid-build — a real bug caught by the test suite, not a hypothetical one), loading/error(+Retry)/empty(+"Start Onboarding") states, the real `myCompanies`/`gameState`/`myPendingActions` combined query (bare top-level auth-scoped fields, not nested under `me` — verified against `Api/Types/Query.Rankings.Performance.cs` and `Api/Types/Query.Operations.cs`), company cards (cash, building count, action buttons to Buy Building/Ledger/Contracts/Research/Settings), a building list per company with status badges (destroyed, loan-default, non-`POWERED` power status) that navigates to `/bank/:id` for `BANK` buildings and `/building/:id` otherwise, and a pending-actions section. Pull-to-refresh re-fetches silently (no full-screen spinner), approximating the web's "no loading-spinner flash on background tick refresh" behavior without needing the web's live tick-polling infrastructure. Covered by `test/dashboard_screen_test.dart` (8 tests: both redirect guards, empty/error/loaded states, per-building-type navigation targets, and silent pull-to-refresh call-count).
  - Deliberately trimmed from the web version (a large, tab-heavy view — see the file's top-of-file comment for the full list): no "Launch New Company" modal, no 5-tab layout (Overview/Buildings/Activity/Chat/Pro — buildings and pending actions are just shown together on one scroll), no Pro subscription panel, no per-city power-grid balance summary strip, no per-building revenue/cost/profit ledger panel or supply-chain unit-status panel, no currency-code-aware cash formatting (plain `$` prefix, not the ledger-derived `primaryCurrencyCode`), and no live tick-based auto-polling refresh (needs a shared game-state/tick subscription this app doesn't have yet). Person-account mode / multi-company switching is **not** trimmed — it's implemented app-wide via the header `ContextSwitcher` (see the entry below) rather than locally on this screen.
- [x] Implement the persistent person/company/city context switcher (`lib/core/widgets/context_switcher.dart` `ContextSwitcher`, mirrors `components/layout/ContextSwitcher.vue` + `ContextSwitcherPanel.vue`) — a single, always-visible header control (mounted in `AppShell`'s app bar whenever the player is signed in) replacing what was previously a set of scattered, inconsistent per-screen "pick a company" dropdowns (each independently defaulting to `companies.first`, with no relationship to each other or to any persistent notion of "who am I acting as"). Backed by `lib/core/context/account_context_state.dart` `AccountContextState` (a `ChangeNotifier` provided at the app root, mirroring `stores/auth.ts`'s `activeAccountType`/`activeCompanyId`/`selectedCityId`) and `account_context_service.dart` `AccountContextService`, which wraps the real `personAccount`/`myCompanies`/`cities` queries and the real `switchAccountContext` mutation (verified against `Api/Types/Inputs.cs`'s `SwitchAccountContextInput` and `Api/Data/Entities/Player.cs`'s `AccountContextType` — `"PERSON"`/`"COMPANY"`). Tapping the trigger opens a bottom sheet listing every city (tap to select — purely client-side/persisted, matching the web: there is no backend "current city" concept, only `ActiveAccountType`/`ActiveCompanyId` on `Player`) and every account (Person + each owned company, with cash shown), with the active one checked. Company switching re-runs the same "which city has the most of my stuff" auto-select heuristic as the web's `selectMainCity`/`shouldAutoSwitchCity` (computed from each company's building city IDs, since the backend has no per-city building-count query — see `_autoSelectCityIfNeeded` in `account_context_state.dart`). The active company (once switching is meaningful, not just cosmetic) now also drives the default "borrowing company"/"buyer company" selection on `bank_loan_request_screen.dart` and `building_market_screen.dart`'s Make Offer dialog, instead of always defaulting to `companies.first` — the clearest starting points for wiring the remaining scattered per-screen company pickers (`city_tab_screens.dart`'s contract bid dialog, `market_screens.dart`, `contracts_screen.dart`) the same way in a future pass.
- [x] Implement the News screen (`lib/features/news/news_screen.dart` `NewsScreen` + `news_models.dart`/`news_service.dart`, mirrors `NewsView.vue`): the real `gameNewsFeed(includeDrafts: false)` query and `markAllGameNewsRead` mutation (verified against `Api/Types/Query.News.cs`), type filter chips (All/News/Changelog/Market Report), client-side pagination (10/page), locale-aware localization fallback (`requested → en → first available`, mirroring `pickGamesLocalization`), and fire-and-forget auto-mark-all-read on load when authenticated with unread items (failures swallowed, matching the web). Entry bodies render as plain text via `lib/core/utils/html_text.dart`'s `stripHtmlToText` rather than full rich HTML — a deliberate, documented simplification, not an oversight (see that file's doc comment). Covered by `test/news_screen_test.dart` (5 tests: entries+auto-mark-read, no-auto-mark when unauthenticated, filter chip, error+retry, mark-all-read confirm dialog).
- [x] Implement the Notifications screen (`lib/features/news/notifications_screen.dart` `NotificationsScreen` + `notification_models.dart`/`notifications_service.dart`, mirrors `NotificationsView.vue`): the real `myNotifications`/`notificationCount` queries and `markNotificationsRead`/`markAllNotificationsRead` mutations (verified against `Api/Types/Query.Notifications.cs` — the two-query/two-mutation contract the web's store actually uses, not the alternative single combined `playerNotificationInbox` query the backend also exposes), day-grouped list, fire-and-forget auto-mark-read-on-load for unread items, and click-to-navigate implementing `handleNotificationClick`'s exact priority order (`buildingId` → shipment/logistics types → loan types/`loanId` → bank-balance types/`bankAccountId` → takeover/`companyId` → `/dashboard` fallback). Covered by `test/notifications_screen_test.dart` (4 tests: grouped list+auto-mark-read, error+retry, buildingId click-navigation, mark-all-read).
- [x] Implement the Contracts screen (`lib/features/economy/contracts_screen.dart` + `contracts_models.dart`/`contracts_service.dart`, mirrors `ContractsView.vue`): the real `myContracts`/`me{companies}`/`companyRankings` queries and `proposeSupplyContract`/`acceptSupplyContract`/`rejectSupplyContract`/`cancelSupplyContract` mutations (verified against `Api/Types/Query.SupplyContracts.cs` / `Mutation.SupplyContracts.cs`), a create-offer form matching the web's fields exactly including its free-text UUID inputs for seller building unit / resource type / product type (the web has no dropdown lookups for these either — not a mobile-specific trim), and three status columns (Pending with Accept/Reject, Active with a delivery health badge + Cancel, History read-only) with the exact `deliveryBadge` logic ported (`penaltyCount > 0` → error, `totalUndeliveredQuantity > 0` → warning, else ok). Covered by `test/contracts_screen_test.dart` (6 tests: three-column rendering, error+retry, accept/reject/cancel actions, and the create-offer form's enable/submit-args flow).
- [x] Implement the Leaderboard screen (`lib/features/leaderboard/leaderboard_screens.dart` `LeaderboardScreen` + `leaderboard_models.dart`/`leaderboard_service.dart`, mirrors `LeaderboardView.vue`): Players/Companies tabs (companies lazily fetched on first switch, matching the web), the real `rankings`/`companyRankings`/`endgameStatus` queries (verified against `Api/Types/Query.Types.Rankings.cs`/`Query.Types.Endgame.cs`), medal rank badges, compact badge-type icons (`profileBadgeIcons`, ported from `lib/profileBadges.ts`'s catalog), client-side pagination (10/page), and the endgame race-to-the-top benchmark card. Tapping a row navigates to `/player/:id`. Trimmed (documented in-file): no "You" row-highlighting (`AuthState` doesn't track the player's own id yet — nothing else needed it), no external "view master ranking" link, no `?tab=&page=` URL persistence.
- [x] Implement the Player Profile screen (`lib/features/leaderboard/leaderboard_screens.dart` `PlayerProfileScreen` + shared `leaderboard_models.dart`/`leaderboard_service.dart`, mirrors `PlayerProfileView.vue` + `components/profile/PlayerProfileTabsContent.vue`): the real `playerProfile`/`playerBadges`/`rankHistory` queries (verified against `Api/Types/Query.Profile.cs`/`Query.PlayerProfile.cs`), header (display name, Pro badge, join year, rank badge, read-only bio), quick-stats row, and Overview (industries, sales stats, hall of fame) / Achievements (badge grid) / Rank History (list, lazily fetched per tab) tabs. Deliberately trimmed (all documented in-file, not oversights): bio/display-name editing and the session-security panel (both write via the Master API or REST session endpoints the web's *own*-profile view exposes — this screen only ports the "view" half), the SVG rank-history chart (rendered as a plain list instead — no charting dependency added), and CSV/PDF stats export (desktop file-download/print flows with no direct mobile equivalent).
- [x] Implement the Cities screen (`lib/features/cities/cities_screens.dart` `CitiesScreen` + `cities_models.dart`/`cities_service.dart`, mirrors `CitiesView.vue`): the real `cities` query (verified against `Api/Types/Query.World.cs`), client-side sort by population descending, formatted population (`1.2M`/`15K`), top-4 resources by abundance with the ported `RESOURCE_ICONS_BY_SLUG` emoji lookup, and a "View city map" button per card navigating to `/city/:id`. Trimmed: country flags render as a plain text chip instead of `CountryFlag.vue`'s flag images (no flag-icon asset/package added).
- [x] Implement the World Map screen (`lib/features/cities/cities_screens.dart` `WorldMapScreen`, mirrors `WorldMapView.vue`): the real `getCities` expansion-overview query (verified against `Api/Types/Query.MultiCityExpansion.cs` — a distinct GraphQL name from the plain `cities` query above, with server-computed `isUnlocked`/`progressPercent`/`requiredNetWorth`), a selectable city list with a detail panel (population, currency, available land, competition, featured resource, unlock progress bar), "Go to city" for unlocked cities, and "Start expanding" for locked ones (redirects to `/login` if unauthenticated, otherwise to `/ledger/:companyId` using the player's first company via a small `me { companies { id } }` lookup, or `/dashboard` if they have none — mirrors `startExpanding()`). Deliberately trimmed (documented in-file): the interactive Leaflet map (marker pins, tile layer, fly-to animation) is a plain sortable list instead — the same "no mapping library for one screen" call as Onboarding's list-based lot picker.
- [x] Implement the Building Market screen (`lib/features/buildings/building_market_screen.dart` `BuildingMarketScreen` + `building_market_models.dart`/`building_market_service.dart`, mirrors `BuildingMarketView.vue`): the real `buildingMarket`/`myBuildingListings` queries and `makeOfferOnBuilding`/`acceptBuildingOffer`/`rejectBuildingOffer` mutations (verified against `Api/Types/Query.BuildingMarket.cs`/`Mutation.BuildingMarket.cs`), a public-listings tab with a city filter and a Make Offer dialog, and an auth-only My Listings tab showing incoming pending offers with Accept/Reject. Trimmed: no negotiation-note field on the offer dialog, no auto-refresh-and-retry UX on `OFFER_VERSION_CONFLICT` beyond a plain error message (the mutation itself is still the real version-checked one).
- [x] Implement the Buy Building screen (`lib/features/buildings/buy_building_screen.dart` `BuyBuildingScreen` + `buy_building_models.dart`/`buy_building_service.dart`, mirrors `BuyBuildingView.vue`): a 4-step flow (city → building type → lot → confirm) against the real `cities`/`cityLots`/`purchaseLot` operations (verified against `Api/Types/Inputs.Building.cs`'s `PurchaseLotInput`). Trimmed: the interactive map-based lot picker is a plain price-sorted list instead (same call as World Map/Onboarding's list-based lot pickers); no BANK-specific follow-up mutations (`setBankRates`/`initiateBaseDeposit`); no POWER_PLANT subtype or MEDIA_HOUSE channel-type sub-selection.
- [x] Implement the Sell Building screen (`lib/features/buildings/sell_building_screen.dart` `SellBuildingScreen` + `sell_building_models.dart`/`sell_building_service.dart`, mirrors `SellBuildingView.vue`): market valuation display, list-for-sale/update-price/cancel-listing via `setBuildingForSale`, and a destroy-with-confirmation flow via `destroyBuilding` (verified against `Api/Types/Inputs.Building.cs`), with a collateralized-building lockout matching the server's `BUILDING_LOCKED_AS_COLLATERAL` rule. No dedicated per-building query exists server-side — like the web, this loads all `myCompanies.buildings` and finds the target client-side.
- [x] Implement the Building Detail screen (`lib/features/buildings/building_detail_screen.dart` `BuildingDetailScreen` + `building_detail_models.dart`/`building_detail_service.dart`, mirrors `BuildingDetailView.vue`) — **heavily trimmed, documented extensively in-file**: this web view is ~5600 lines with a drag/link-cycle grid unit editor, media-house/power-plant control panels, and a dozen analytics dashboards; the mobile port covers building overview (name/type/level/power/occupancy), a read-only unit list with resource/product names resolved via the encyclopedia catalog queries, pending-configuration status, and exactly two real quick-action mutations (`scheduleUnitUpgrade`, `updatePublicSalesPrice`). Explicitly not ported: the full grid configuration editor and `storeBuildingConfiguration`/`cancelBuildingConfiguration`, media-house/power-plant/rent-scheduling mutations, `flushStorage`, all analytics/history panels, and the Global Exchange sourcing/vendor-selector flow — see the file's top-of-file comment for the complete list this was scoped against.
- [x] Implement the Encyclopedia screen (`lib/features/encyclopedia/encyclopedia_screens.dart` `EncyclopediaScreen` + `encyclopedia_models.dart`/`encyclopedia_service.dart`, mirrors `ManufacturingEncyclopediaView.vue`): the real paginated `encyclopediaResources(page)` query (verified against `Api/Types/Query.Encyclopedia.cs`), fetching all pages on load like the web, with local search and a RESOURCE/PRODUCT/ALL filter, Pro-only badge, and navigation to `/encyclopedia/resource/:slug`. Trimmed (documented in-file): only the GraphQL-backed "resources-definition" catalog tab is ported — the web's other 5 topic tabs render static local guide content with no GraphQL (onboarding/factory-layout/sales-shop/forex-trading/stock-exchange help), not core gameplay.
- [x] Implement the Resource Detail screen (`lib/features/encyclopedia/encyclopedia_screens.dart` `ResourceDetailScreen`, mirrors `ResourceDetailView.vue`): the real `encyclopediaResourceDetail(slug)` query with `producedByRecipes`/`usedInRecipes`, a Pro-only filter toggle, and clickable recipe input/output chips that navigate between resource-detail pages, matching the web's cross-linking behavior.
- [x] Implement the Exchange screen (`lib/features/exchange/global_exchange_screen.dart` `GlobalExchangeScreen` + `global_exchange_models.dart`/`global_exchange_service.dart`, mirrors `GlobalExchangeView.vue`): the real `globalExchangeOffers`/`globalExchangeProductListings`/`buyFromExchange` operations (verified against `Api/Types/Inputs.cs`'s `BuyFromExchangeInput`), a Resources tab (destination-city picker + buy dialog resolving the player's STORAGE/PURCHASE units and company bank accounts) and a read-only Products tab (matching the web, which has no direct buy flow there either), now including the web's category/industry filter dropdowns (via `resourceTypes`/`productTypes`) and free-text search on both tabs.
- [x] Implement the Stocks screen (`lib/features/exchange/stock_exchange_screen.dart` `StockExchangeScreen` + `stock_models.dart`/`stock_service.dart`, mirrors `StockExchangeView.vue`): the real `stockExchangeListings` query, tapping a row navigates to Stock Trading, plus dividend proposal/voting (`proposeDividend`/`voteDividendProposal`/`getDividendProposals`), company merger (`mergeCompany`), and CEO replacement/hostile takeover (`replaceCEO`) — gated per-listing on the server-computed `canProposeDividend`/`canMerge`/`canClaimControl` flags. Still deliberately not ported: the web's inline per-row trade panel (buying/selling lives on its own screen here instead of inline).
- [x] Implement the Trade Stock screen (`lib/features/exchange/stock_trading_screen.dart` `StockTradingScreen`, mirrors `StockTradingView.vue`): the real `orderBook`/`stockTradeHistory`/`companyShareholders`/`myOpenOrders`/`buyShares`/`sellShares`/`placeLimitOrder`/`cancelLimitOrder`/`stockExchangePriceHistory`/`personAccount` operations — market buy/sell, limit-order placement with open-order cancellation, order book, recent trades, shareholders list, a price-history sparkline (`SparklineChart`, no charting package dependency), and a position summary panel (shares owned, average buy price, unrealized P&L, available cash) computed client-side exactly like the web's `computeStockPositionSummary` (see `StockPositionSummary.compute` in `stock_models.dart`).
- [x] Implement the Forex screen (`lib/features/exchange/forex_exchange_screen.dart` `ForexExchangeScreen` + `forex_models.dart`/`forex_service.dart`, mirrors `ForexExchangeView.vue`): Swap (quote + execute via `forexQuote`/`executeForexSwap`, now with slippage-tolerance presets and a live quote-countdown timer), Transfer (`bank_transfer_section.dart` `BankTransferSection`, mirrors `BankAccountTransferPanel.vue`, via `transferFunds`), Rates (`fxRates`, plus a rate-history chart via `fxRateHistory`), History (`forexTradeHistory`), and Gold (`gold_amm_section.dart` `GoldAmmSection`, mirrors `GoldAmmSection.vue` — the full AMM: `goldAmmPools`/`myGoldBalance`/`goldAmmSwapQuote`/`executeGoldAmmSwap`/`addGoldAmmLiquidity`/`createGoldAmmPool`/`removeGoldAmmLiquidity`) tabs, plus a commodity-shock event banner (`getActiveMarketEvents`), all behind the same unauthenticated → `/login` redirect guard as Dashboard.
- [x] Implement the City Overview tab (`lib/features/city/city_tab_screens.dart` `CityOverviewScreen` + `city_tab_models.dart`/`city_tab_service.dart`, mirrors `CityOverviewTab.vue`): city stats (population, base salary) and available/total lot counts via the real `city(id)`/`cityLots(cityId)` queries. Architecturally adapted, not trimmed: on the web all 6 tabs are nested components sharing one parent view's (`CityMapView.vue`) fetched state; here each tab is its own go_router screen and fetches its own data — a reasonable fit for this app's routing model.
- [x] Implement the City Economy tab (`lib/features/city/city_tab_screens.dart` `CityEconomyScreen`, mirrors `CityEconomyTab.vue`): basic city currency/population/salary stats. Trimmed: skips the economic-cycle, weather, power-grid-balance, and 365-tick economic-history dashboards (`EconomyCycleWidget`/`CityPowerPlanningSection`/`HealthIndicatorsPanel`) — five distinct analytics subsystems, documented as not-yet-available rather than silently missing.
- [x] Implement the City Buildings tab (`lib/features/city/city_tab_screens.dart` `CityBuildingsScreen`, mirrors `CityBuildingsTab.vue`): a sortable/filterable lot list (available-only toggle) via `cityLots`, tapping an owned lot navigates to its building. Trimmed: the interactive map (`CityMapContent.vue`) is a plain list — the same call as World Map/Buy Building's list-based lot pickers elsewhere in this app.
- [x] Implement the City Market tab (`lib/features/city/city_tab_screens.dart` `CityMarketScreen`, mirrors `CityMarketTab.vue`): the city's resource-abundance list (reusing the `cities`-query shape already used by the Cities screen). Trimmed: no `CityDemandPanel` top-selling-products analytics or `CityMediaHousesSection`.
- [x] Implement the City Contracts tab (`lib/features/city/city_tab_screens.dart` `CityContractsScreen`, mirrors `CityContractsTab.vue`): the real `cityGovernmentContracts`/`contractDetail`/`submitContractBid` operations (verified against `Api/Types/GovernmentContractTypes.cs`), an open-contracts list, and a bid dialog with live eligibility feedback per selected company.
- [x] Implement the City Competitors tab (`lib/features/city/city_tab_screens.dart` `CityCompetitorsScreen`, mirrors `CityCompetitorsTab.vue`): the real `cityCompetitors(cityId, lastNTicks: 10)` query (verified against `Api/Types/Query.Npc.cs`), a company list with market share and trend indicator.
- [x] Implement the Ledger screen (`lib/features/company/company_screens.dart` `LedgerScreen` + `company_models.dart`/`company_service.dart`, mirrors `LedgerView.vue`): the real `companyLedger`/`companyCityFinancialBreakdown` queries (verified against `Api/Types/Query.Ledger.cs`) — P&L summary (revenue/costs by category, net income, assets) and a per-city financial breakdown. Heavily trimmed (documented in-file): the web delegates to `LedgerMainContent.vue` (1002 lines) which adds per-category drill-down rows, cross-city shipment tracking, a city-unlock expansion panel, and multi-year history tables — none of that is ported.
- [x] Implement the Company Contracts screen (`lib/features/company/company_screens.dart` `CompanyContractsScreen`, mirrors `CompanyContractsView.vue`): the seller-side fulfillment dashboard (distinct from the City Contracts tab's open-bidding browser) — the real `companyContracts`/`myContractBids`/`fulfillContractShipment` operations, awarded contracts with a ship-quantity form, and bid history. Simplified from the web's 4-column kanban to two grouped lists (Awarded vs. other).
- [x] Implement the Company Settings screen (`lib/features/company/company_screens.dart` `CompanySettingsScreen`, mirrors `CompanySettingsView.vue`): the real `companySettings`/`updateCompanySettings`/`proposeDividend`/`voteDividend` operations — name/dividend-ratio form, read-only administration overhead/age/asset factors, per-city salary-multiplier sliders, and dividend proposal voting. Trimmed: no `proposeDividend` UI form yet (the mutation is wired in the service but not exposed in this screen's UI).
- [x] Implement the Company Research screen (`lib/features/company/company_screens.dart` `CompanyResearchScreen`, mirrors `CompanyResearchView.vue`): the real `brandQualityOverview` query — total research budget and per-brand combined quality, a small read-only dashboard matching the web closely (no mutations on this screen either way).
- [x] Implement the Personal Ledger screen (`lib/features/company/personal_ledger_screen.dart` `PersonalLedgerScreen` + `personal_ledger_models.dart`/`personal_ledger_service.dart`, mirrors `PersonalLedgerView.vue`): the real `personAccount` query — net wealth/cash/tax-reserve summary, shareholdings, dividend payments, and stock trade history. Trimmed: no endgame race progress bar/leaderboard or milestone toast notifications (tied to a separate `endgameStore`), and no interest-payments history table with its ALL/INTEREST/DIVIDEND filter.
- [x] Implement the Banking (loan marketplace) screen (`lib/features/banking/loan_marketplace_screen.dart` `LoanMarketplaceScreen` + `banking_models.dart`/`banking_service.dart`, mirrors `LoanMarketplaceView.vue`): Borrow (my active loans with a repay-in-full action on OVERDUE/DEFAULTED loans via `repayLoanDebt`, plus an all-banks list restricted to `baseCapitalDeposited` banks sorted by lending rate ascending) and Deposit (my accounts with a withdraw dialog supporting partial or full closure via `closeBankAccount`/`closeCompanyBankAccount`, plus an all-banks list with a city filter, "available only" checkbox, and a 4-way sort toggle) tabs, against the real `myLoans`/`allBanks`/`myBankAccounts`/`openBankAccount`/`closeBankAccount`/`closeCompanyBankAccount`/`repayLoanDebt` operations (verified against `Api/Types/Inputs.Banking.cs`/`Inputs.Lending.cs`).
- [x] Implement the Bank Management screen (`lib/features/banking/bank_management_screen.dart` `BankManagementScreen`, mirrors `BankManagementView.vue`): the real `bankInfo`/`bankLoans`/`bankDeposits`/`setBankRates`/`initiateBaseDeposit`/`updateBankDepositRate`/`bankDepositRateHistory`/`repayLoanDebt` operations, including a liquidity health panel (available cash, reserve requirement/shortfall, central bank debt/rate, liquidity status) and a scheduled deposit-rate-change form with an expandable rate-history audit trail. Adapted rather than trimmed: the web's separate owner (`BankManagementTabContent.vue`) and customer (`BankCustomerView.vue`) sub-views are folded into one screen driven by a simple ownership check (`bankInfo.lenderCompanyId` against the player's companies).
- [x] Implement the Request Loan screen (`lib/features/banking/bank_loan_request_screen.dart` `BankLoanRequestScreen`, mirrors `BankLoanRequestView.vue`): a single form (borrowing company → optional collateral building from `myCollateralBuildings` → principal amount → duration in ticks, default 8760 matching the web → settlement account from `companyBankAccounts`) against the real `acceptLoan` mutation (verified against `Api/Types/Inputs.Lending.cs`'s `AcceptLoanInput` — note the web passes the bank's own building ID as `loanOfferId` for direct bank borrowing, no separate offer entity). Simplified from the web's 4-step wizard into one screen.
- [x] Implement the Bank Statement screen (`lib/features/banking/bank_statement_screen.dart` `BankStatementScreen`, mirrors `BankStatementView.vue`): the real paginated `bankStatement` query with an account selector, page-size selector (20/50/100/200), fromTick/toTick range filter, and prev/next pagination, balance summary and transaction rows, with the same unauthenticated → `/login` redirect guard as Dashboard/Forex.
- [x] Implement the Campaigns / Market Intelligence screen (`lib/features/market/market_screens.dart` `MarketIntelligenceScreen` + `market_models.dart`/`market_service.dart`, mirrors `MarketIntelligenceView.vue`): the real `marketIntelligence(cityId)` query — city picker and per-product seller rankings.
- [x] Implement the Market Dashboard screen (`lib/features/market/market_screens.dart` `MarketDashboardScreen`, mirrors `MarketDashboardView.vue`): the real `marketOverview`/`competitorQualityIntelligence` queries — product table with satisfaction rate, tap-to-expand competitor quality/price comparison. Trimmed: no price-history chart panel (`MarketPriceHistoryPanel.vue`'s bar chart isn't ported — no charting dependency added).
- [x] Implement the Energy Market screen (`lib/features/market/market_screens.dart` `EnergyMarketScreen`, mirrors `EnergyMarketView.vue`): the real `energyMarket`/`listEnergyForSale`/`cancelEnergyListing` operations (verified against `Api/Types/Query.EnergyMarket.cs`/`Mutation.EnergyMarket.cs`), city filter, and a "List Surplus" dialog for the player's power plants. Adapted rather than trimmed: fetches all cities' listings in parallel and filters client-side instead of the web's per-city N+1 query loop.
- [x] Implement the Global Events screen (`lib/features/market/market_screens.dart` `GlobalEventsScreen`, mirrors `GlobalEventsPanel.vue`): the real `activeGlobalEvents`/`globalEventHistory` queries, Active/History tabs with severity-badged cards. The web's admin "Trigger Event" button is a non-functional stub even there, so it isn't ported.
- [x] Implement the Marketing Analytics screen (`lib/features/market/market_screens.dart` `MarketingAnalyticsScreen`, mirrors `MarketingAnalyticsView.vue`): the real `campaignAnalytics` query — company selector (when the player has more than one), summary stats, and per-unit brand/marketing rows. Trimmed: plain rows instead of `MarketingAnalyticsContent.vue`'s trend/recommendation badge styling.
- [x] Implement the Trade Routes screen (`lib/features/trade/trade_screens.dart` `TradeRoutesScreen` + `trade_models.dart`/`trade_service.dart`, mirrors `TradeRoutesView.vue`): the real owner-scoped `myTradeRoutes` query (verified against `Api/Types/Query.TradeRoutes.cs`), an active-shipment summary, and All/Active/Completed filter tabs.
- [x] Implement the Tutorial screen (`lib/features/tutorial/tutorial_screen.dart` + `tutorial_models.dart`/`tutorial_service.dart`, mirrors `TutorialView.vue`): the real `tutorialProgress` query, all 7 milestones with completion/bounty state and Resume links, progress bar, and a sign-in prompt for guests. Milestone copy is hardcoded English rather than routed through i18n keys (this app's i18n only covers a handful of screens so far) — not a functional trim.
- [x] Implement the Operations Overview screen (`lib/features/operations/operations_screens.dart` `OperationsOverviewScreen` + `operations_models.dart`/`operations_service.dart`, mirrors `OperationsOverviewView.vue`): the real `gameAdminSession`/`gameAdminDashboard` queries — money-supply/cash/inflow/shipping metric cards. **Heavily and deliberately trimmed to read-only** (documented extensively in-file): player impersonation, global/local admin role grants, NPC competitor pause/resume, manually ending the game shard, and player chat-visibility toggling are all sensitive server-administration mutations not ported in this first mobile pass. All 6 Operations screens gate on `gameAdminSession.canAccessAdminDashboard` server-side, not just the drawer's locally-set admin flag.
- [x] Implement the Operations Money Flow screen (`lib/features/operations/operations_screens.dart` `OperationsMoneyFlowScreen`, mirrors `OperationsStatisticsView.vue`): the real `operationsStatistics(range)` query with a time-range selector and inflow/outflow category breakdowns.
- [x] Implement the Operations Product Analytics screen (`lib/features/operations/operations_screens.dart` `OperationsProductAnalyticsScreen`, mirrors `OperationsAnalyticsView.vue`): the real `adminProductAnalytics` query — per-product server-wide sales/revenue rows. Trimmed: no CSV export.
- [x] Implement the Operations News screen (`lib/features/operations/operations_screens.dart` `OperationsNewsScreen`, mirrors `OperationsNewsView.vue`): the real `gameNewsFeed(includeDrafts: true)` query, read-only (the web's full compose/edit/publish CMS form via `upsertGamesEntry` is not ported).
- [x] Implement the Operations Players screen (`lib/features/operations/operations_screens.dart` `OperationsPlayersScreen`, mirrors `OperationsPlayersView.vue`): reuses `gameAdminDashboard.players` (no dedicated players query exists server-side, matching the web) with search filtering and tap-through to player detail.
- [x] Implement the Operations Player Detail screen (`lib/features/operations/operations_screens.dart` `OperationsPlayerDetailScreen`, mirrors `OperationsPlayerDetailView.vue`): looks up the player from `gameAdminDashboard.players` by ID (matching the web's client-side lookup — no dedicated single-player query exists either). Trimmed: no impersonation, admin-role-toggle, or chat-visibility-toggle actions — this screen is view-only.

### Flutter mobile app — Building Detail is not functionally playable yet

The web's `BuildingDetailView.vue` (via `useBuildingDetail.ts`, ~5600 lines) is the core gameplay loop — laying out and wiring a factory/shop. `building_detail_screen.dart` currently only renders a flat read-only unit list with two quick-action mutations; none of the actual factory-building gameplay below exists on mobile yet, so buildings are not fun/playable on the Flutter app the way they are on web.

- [x] Port the 4×4 grid unit editor layout (`lib/features/buildings/building_unit_grid.dart` `BuildingUnitGrid`, mirrors the read-only "Active Configuration" grid in `BuildingUnitGrid.vue`/`useBuildingDetail.ts`'s `gridIndexes = [0,1,2,3]`): units now render at their real `gridX`/`gridY` (each verified 0-3 against `BuildingConfigurationService.Validation.cs`'s `ValidateUnits`) in a 4×4 grid instead of a flat `ListView`, for every building type except APARTMENT/COMMERCIAL/MEDIA_HOUSE (matches `isMultiUnitBuilding` in `BuildingDetailView.vue`) — empty cells render as non-interactive placeholders, occupied cells are color-tinted per unit type (`unitTypeColors`, ported from `unitColors`) and tapping one opens a bottom sheet with the existing quick actions. Read-only layout port only — placing/removing units, the 8-directional link editor, the draft/planned grid, per-unit config editing, and inventory fill bars are intentionally separate, still-open ROADMAP items below (each was already a distinct line item and is unaffected by this one).
- [ ] Implement placing a new unit on an empty grid cell (tap-to-open unit picker with per-type cost preview, mirroring `BuildingEditingSidebar.vue`'s `showUnitPicker`/`placeUnit`) — there is currently no way to add a unit to a building on mobile.
- [ ] Implement the 8-directional unit link system: horizontal, vertical, and both diagonals (`linkUp`/`linkDown`/`linkLeft`/`linkRight`/`linkUpLeft`/`linkUpRight`/`linkDownLeft`/`linkDownRight`, toggled via `toggleHorizontalLink`/`toggleVerticalLink`/`togglePrimaryDiagonalLink`/`toggleSecondaryDiagonalLink`) — this is the "directional links" mechanic that connects purchase→manufacturing→storage→sales units, and it is entirely absent from Flutter.
- [ ] Port link-state/flow arrow visuals (`UnitLinkArrow.vue`, `DiagonalConnector.vue`, `getHorizontalLinkStateFor`/`isHorizontalLinkLive`) so players can see which units are wired together and which links are actively carrying flow.
- [ ] Implement the planned/draft grid separate from the active (applied) grid, with `storeBuildingConfiguration`/`cancelBuildingConfiguration` mutations, instead of the current two isolated one-shot mutations (`scheduleUnitUpgrade`, `updatePublicSalesPrice`) with no draft/preview/save/cancel cycle.
- [ ] Add per-unit configuration editing (Config/Energy/Performance/Maintenance tabs from `UnitConfigurationTabView.vue` + `BuildingUnitConfigFields.vue`): resource/product selection, min/max price, min quality, marketing budget/brand scope, B2B suggested-price hints — Flutter shows unit type/level/item name only, with no way to edit any of it.
- [ ] Add a "remove unit from grid" action (`removeUnit`/config-tab remove button) — currently units can only be upgraded, never removed or replaced.
- [ ] Show draft-change summaries before committing (link changes list, unit added/removed/replaced list, total ticks, total construction cost, projected company cash after apply).
- [ ] Implement per-unit upgrade preview (`unitUpgradeInfo`: current/next level, storage capacity delta, labor cost delta, energy cost delta) before confirming — today's "Upgrade" button has no preview of what it does.
- [ ] Add unit inventory/capacity visualization per cell (fill bar with last-tick inflow/outflow segments, quantity/capacity, sourcing cost, average quality) — Flutter shows no inventory state at all, just the configured item name.
- [ ] Implement unit clipboard copy/paste (desktop Ctrl+C/Ctrl+V and mobile copy/paste buttons, per `unitClipboard.ts`) to speed up laying out repeated unit configurations.
- [ ] Port supply-chain completeness validation and status panel (`buildingChainValidation.ts` reachability check + `BuildingChainStatusPanel.vue`) so mobile players get "your production chain isn't connected end-to-end" guidance like web does.
- [ ] Implement the starter-layout one-click setup banners (`applyStarterLayout`/`applyShopStarterLayout`) that guide new players through configuring their first factory/shop.
- [ ] Port the Media House control panel (content budget, channel/unit config, competitor/effectiveness/brand analytics, income history — `BuildingMediaHousePanel.vue`, `setMediaHouseContentBudget`/`upgradeMediaHouse`/`configureMediaHouseUnit`) — MEDIA_HOUSE buildings have no dedicated management UI on mobile.
- [ ] Port the Power Plant control panel (dispatch % slider, power priority, spot-market energy listing/cancel/max-bid-price, analytics — `BuildingPowerPlantPanel.vue`, `setPlantDispatch`/`setPowerPriority`/`listEnergyForSale`/`cancelEnergyListing`/`setMaxEnergyBidPrice`).
- [ ] Port the Research & Development panel (per-unit brand quality overview — `BuildingResearchPanel.vue`).
- [ ] Port the Apartment/Commercial property panel (rent-per-sqm setting, occupancy detail — `BuildingPropertyPanel.vue`, `setRentPerSqm`, `apartmentBuildingDetail`).
- [ ] Add PUBLIC_SALES-specific tools: sales analytics, low-inventory alert threshold, and "flush storage" (`publicSalesAnalytics`, `setPublicSalesInventoryAlertThreshold`, `flushStorage`).
- [ ] Add the Global Exchange sourcing/vendor-selector for PURCHASE units (cross-city offer comparison with transit cost/quality/blocked-price hints — `sourcingCandidates`/`globalExchangeOffers`/`procurementPreview`).
- [ ] Add building analytics panels: unit resource history charts, financial timeline, recent activity feed, and the supply-chain diagram (`buildingUnitResourceHistories`, `buildingFinancialTimeline`, `buildingRecentActivity`, `buildingSupplyChain`, `unitProductAnalytics`).
- [ ] Surface active market-event banners scoped to the building's city (`getActiveMarketEvents`) and first-time-user grid-editor tutorial tooltips.
- [ ] Add the "remove destroyed building" action (`removeDestroyedBuilding`) for destroyed buildings still listed on the dashboard.

### Flutter mobile app — other documented feature-parity gaps vs. web

Every screen implemented so far documents its own trims in-file and in the checklist above; this groups the gameplay-relevant ones (cosmetic-only trims like flag icons or chart styling are omitted) so they can be tracked and picked off individually. The goal is functional parity — the same things must be doable from a phone as from a desktop browser.

- [ ] Dashboard: add the "Launch New Company" flow, Pro subscription panel, per-city power-grid balance summary, per-building revenue/cost/profit ledger + supply-chain status panel, and live tick-based auto-refresh (`DashboardView.vue`'s 5-tab layout vs. the current single-scroll list).
- [ ] Onboarding: replace the list-based lot picker with the interactive map + district-name/population-index recommended-lot heuristic, add FX-accurate currency conversion, wordlist-based company/person name generation, and the auto-polled first-sale-mission celebration panel; wire up real Pro-subscription gating instead of always blocking Pro industries.
- [ ] World Map / Buy Building / City Buildings tab: replace the plain sortable lot lists with the interactive map (marker pins, fly-to, distance-to-existing-buildings) used on web in all three flows.
- [ ] Buy Building: add BANK-specific follow-up setup (`setBankRates`/`initiateBaseDeposit`), POWER_PLANT subtype selection, and MEDIA_HOUSE channel-type selection during purchase.
- [ ] Building Market: add the offer negotiation-note field and auto-refresh-and-retry UX on `OFFER_VERSION_CONFLICT`.
- [ ] Encyclopedia: port the 5 static guide tabs (onboarding, factory-layout, sales-shop, forex-trading, stock-exchange help) beyond the current resources-only catalog tab.
- [ ] Stocks: add the inline per-row trade panel so buying/selling doesn't require navigating to a separate screen.
- [ ] City Economy tab: port the economic-cycle, weather, power-grid-balance, and 365-tick economic-history dashboards.
- [ ] City Market tab: add the top-selling-products demand panel and the city media-houses section.
- [ ] Ledger: add per-category drill-down rows, cross-city shipment tracking, the city-unlock expansion panel, and multi-year history tables.
- [ ] Company Settings: expose the `proposeDividend` form in the UI (mutation is already wired in the service).
- [ ] Personal Ledger: add the endgame race progress bar/leaderboard with milestone toasts, and the interest-payments history table with its ALL/INTEREST/DIVIDEND filter.
- [ ] Market Dashboard: add the price-history chart panel per product.
- [ ] Player Profile: add bio/display-name editing and the session-security panel for the player's own profile.
- [ ] Operations screens: wire up player impersonation, admin-role grants, NPC competitor pause/resume, ending the game shard, chat-visibility toggling, and the News screen's compose/edit/publish CMS form — currently every Operations screen is deliberately read-only.
- [ ] Leaderboard: highlight the signed-in player's own row once `AuthState` tracks the player's id, and persist the selected tab/page in the URL like web does.
