- MasterApi draft news reads currently trust a caller-supplied `RequesterEmail` without requiring trusted service credentials or authenticated admin identity: Require validated server credentials or authenticated root/global admin claims before returning unpublished entries, and add regression tests for anonymous draft-read rejection.
- MasterApi news upsert currently authorizes from spoofable `RequesterEmail` and a caller-controlled `ServerKey`: Bind news write authorization to real admin claims or trusted service credentials, reject caller-supplied identity spoofing, and cover global/server-scoped publish paths with negative tests.
- Object-level authorization responses in economy-sensitive mutations still reveal foreign-object state and precise balances: Standardize unauthorized paths to `not found or not owned` semantics and suppress competitor-balance detail while keeping full internal security logs.
- The game frontend production dependency graph contains known advisories through `dompurify`, `@vercel/node`, `undici`, `path-to-regexp`, `minimatch`, and `postcss`: Upgrade or replace vulnerable packages, remove server-only packages from shipped client dependencies, and gate releases with `npm audit --omit=dev`.
- Rich HTML rendering remains a sensitive surface in the news flow: Keep backend sanitization authoritative, update sanitization libraries promptly, and test stored HTML rendering for safe handling of hostile content.